/**
 * UI Refresh shared atoms — barrel export.
 * Consumed by the batch-3 page ports.
 */
export {
  Pill,
  Eyebrow,
  SectionH,
  PageHero,
  Sparkline,
  KPI,
  BurnMeter,
  BackBar,
  DetailLayout,
  SideCard,
  MetaRow,
} from './atoms';
export type { RagTone } from './atoms';
